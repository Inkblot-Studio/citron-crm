/**
 * Shared surfaces aligned with EmailCampaignsView / SearchBar / Citron dialogs:
 * soft elevation, restrained borders, spacing tokens — no heavy framing.
 */
export const MODULE_DIALOG_SURFACE_CLASS =
  'gap-0 rounded-xl border border-[var(--inkblot-semantic-color-border-default)] bg-[var(--inkblot-semantic-color-background-secondary)] p-[var(--inkblot-spacing-6)] shadow-[var(--inkblot-shadow-sm)]'

/** Typical gap between PageHeader, toolbar row, and main content (Email Campaigns density). */
export const MODULE_SECTION_GAP_CLASS = 'gap-[var(--inkblot-spacing-6)]'
